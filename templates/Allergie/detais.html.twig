<div class="container-fluid bg-light bg-icon py-6">
    <div class="container">
        <div class="section-header text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 500px;">
            <h1 class="display-5 mb-3">Détails de l'Allergie</h1>
            <p>Informations détaillées sur l'allergie sélectionnée.</p>
        </div>
        <div class="row g-4">
            <div class="col-lg-12">
                <div class="bg-white text-center p-4 p-xl-5">
                    <h4 class="mb-3">{{ allergies.nom }}</h4>
                    <p class="mb-4">{{ allergies.description }}</p>
                    <a class="btn btn-outline-primary border-2 py-2 px-4 rounded-pill" href="{{ path('liste_allergies') }}">Retour à la liste</a>
                </div>
            </div>
        </div>
    </div>
</div>
