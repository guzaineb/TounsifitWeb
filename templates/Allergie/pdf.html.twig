<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Allergie PDF</title>
    {% block stylesheets %}
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        h1 {
            color: #333;
            border-bottom: 1px solid #333;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .Allergie-item {
            margin-bottom: 20px;
        }
        .Allergie-label {
            font-weight: bold;
            margin-bottom: 5px;
        }
        img.logo {
            width: 40px;
            height: 40px;
            border-radius: 50%;
        }
    </style>
    {% endblock %}
</head>
<body>
    <div class="container">
        <a href="index.html" class="navbar-brand ms-4 ms-lg-0">
            <h6 class="fw-bold text-primary m-0">Tou<span class="text-secondary">nsi</span>fit</h1>
        </a>
        <h1>{{ allergie.nom }}</h1>
        <div class="Allergie-item">
            <div class="Allergie-label">Description:</div>
            <div class="Allergie-value">{{ allergie.Description }}</div>
        </div>
    </div>
</body>
</html>
